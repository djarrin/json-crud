import React from 'react';
import PropTypes from 'prop-types';

const JsonEditForm = props => {
  return (
    <div>

    </div>
  );
};

JsonEditForm.propTypes = {
  onSubmit: PropTypes.func.isRequired
};

export default JsonEditForm;
